dependencies:
  pre:
    - gem update --system
    - gem update bundler
database:
  override:
    - createdb dummy_test
test:
  override:
    - bundle exec rake
  post:
    - bundle exec codeclimate-test-reporter
